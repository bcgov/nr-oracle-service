---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nr-oracle-service.name" . }}
  labels: 
    {{- include "nr-oracle-service.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels: 
      {{- include "nr-oracle-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "nr-oracle-service.selectorLabels" . | nindent 8 }}
    spec:
      volumes:
        - name: {{ include "nr-oracle-service.name" . }}
          persistentVolumeClaim:
            claimName: {{ include "nr-oracle-service.name" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      initContainers:
        - name: {{ .Chart.Name }}-init
          image: "image-registry.openshift-image-registry.svc:5000/{{.Release.Namespace}}/{{ include "nr-oracle-service.fullname" . }}-init:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  key: db-host
                  name: {{ include "nr-oracle-service.name" . }}
            - name: CERT_SECRET
              valueFrom:
                secretKeyRef:
                  key: cert-secret
                  name: {{ include "nr-oracle-service.name" . }}
            - name: DB_PORT
              value: "1543"
          volumeMounts:
            - name: {{ include "nr-oracle-service.name" . }}
              mountPath: /app/cert
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "image-registry.openshift-image-registry.svc:5000/{{.Release.Namespace}}/{{ include "nr-oracle-service.fullname" . }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.app.envs.HTTP_PORT }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          env:
            - name: KUBERNETES_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: db-password
                  name: {{ include "nr-oracle-service.name" . }}
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  key: db-user
                  name: {{ include "nr-oracle-service.name" . }}
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  key: db-name
                  name: {{ include "nr-oracle-service.name" . }}
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  key: db-host
                  name: {{ include "nr-oracle-service.name" . }}
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  key: api-key
                  name: {{ include "nr-oracle-service.name" . }}
            - name: DB_PORT
              value: '1543'
            - name: CERT_SECRET
              valueFrom:
                secretKeyRef:
                  key: cert-secret
                  name: {{ include "nr-oracle-service.name" . }}
            - name: POOL_MIN_SIZE
              value: {{ required "A valid .Values.app.envs.POOL_MIN_SIZE entry required!" .Values.app.envs.POOL_MIN_SIZE  | quote }}
            - name: HTTP_PORT
              value: {{ required "A valid .Values.app.envs.HTTP_PORT entry required!" .Values.app.envs.HTTP_PORT  | quote }}
            - name: POOL_IDLE_TIMEOUT
              value: {{ required "A valid .Values.app.envs.POOL_IDLE_TIMEOUT entry required!" .Values.app.envs.POOL_IDLE_TIMEOUT  | quote }}
            - name: POOL_INITIAL_SIZE
              value: {{ required "A valid .Values.app.envs.POOL_INITIAL_SIZE entry required!" .Values.app.envs.POOL_INITIAL_SIZE  | quote }}
            - name: POOL_MAX_SIZE
              value: {{ required "A valid .Values.app.envs.POOL_MAX_SIZE entry required!" .Values.app.envs.POOL_MAX_SIZE  | quote }}
            - name: POOL_MAX_LIFETIME
              value: {{ required "A valid .Values.app.envs.POOL_MAX_LIFETIME entry required!" .Values.app.envs.POOL_MAX_LIFETIME  | quote }}
            - name: POOL_LEAK_DETECTION_INTERVAL
              value: {{ required "A valid .Values.app.envs.POOL_LEAK_DETECTION_INTERVAL entry required!" .Values.app.envs.POOL_LEAK_DETECTION_INTERVAL  | quote }}
          volumeMounts:
            - name: {{ include "nr-oracle-service.name" . }}
              mountPath: /app/cert