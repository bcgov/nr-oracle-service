name: PR

on:
  pull_request:
  merge_group:

concurrency:
  # Cancel in progress for PR open and close, but not merge_group
  group: ${{ github.event.number }}
  cancel-in-progress: true

permissions: {}

jobs:
  pr-greeting:
    name: PR Greeting
    if: "!github.event.pull_request.head.repo.fork"
    runs-on: ubuntu-24.04
    permissions:
      pull-requests: write
    steps:
      - name: PR Greeting
        uses: bcgov/actions/pr-description-add@9f9c481afcfd97314f0b994222ef602ef8fbe2bd # post-#219 main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          add_markdown: |
            ---
            Thanks for the PR!

            This PR deploys to namespace `${{ vars.OC_NAMESPACE }}` as release `${{ github.event.repository.name }}-${{ github.event.number }}`.

            Once merged, code will be promoted via the [Merge Workflow](https://github.com/${{ github.repository }}/actions/workflows/merge.yml).

  builds:
    name: Builds
    if: "!github.event.pull_request.head.repo.fork"
    runs-on: ubuntu-24.04
    permissions:
      packages: write
    strategy:
      matrix:
        package: [nr-oracle-service, nr-oracle-service-init]
        include:
          - package: nr-oracle-service
            build_file: Dockerfile
            build_context: .
            triggers: |
              src/
              pom.xml
          - package: nr-oracle-service-init
            build_file: Dockerfile.certs
            build_context: .
            triggers: |
              get_certs.sh
              Dockerfile.certs
    steps:
      - uses: actions/checkout@v7
      - uses: bcgov/actions/builder@9f9c481afcfd97314f0b994222ef602ef8fbe2bd # post-#219 main
        with:
          package: ${{ matrix.package }}
          tags: ${{ github.event.number }}
          build_file: ${{ matrix.build_file }}
          build_context: ${{ matrix.build_context }}
          triggers: ${{ matrix.triggers }}

  track:
    name: Resolve Images
    needs: [builds]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: read
      pull-requests: read
    outputs:
      digest: ${{ steps.tracker.outputs.digest }}
      digests: ${{ steps.tracker.outputs.digests }}
      images: ${{ steps.tracker.outputs.images }}
    steps:
      - uses: actions/checkout@v7
      - name: Resolve Images
        id: tracker
        uses: bcgov/actions/image-tracker@9f9c481afcfd97314f0b994222ef602ef8fbe2bd # post-#219 main
        with:
          package: nr-oracle-service, nr-oracle-service-init
          # PR HEAD was just built; default max_depth 1 is enough

  deploys:
    name: Deploys
    needs: [builds, track]
    runs-on: ubuntu-24.04
    environment: test
    env:
      project_name: nr-oracle
      app_name: nr-oracle-service
      environment: development
    steps:
      - uses: actions/checkout@v7

      - name: Deploy to Openshift
        uses: bcgov/action-oc-runner@111868d1fc50db0a40417ba321d865ef5c931bbd # v1.7.0
        with:
          oc_namespace: ${{ vars.OC_NAMESPACE }}
          oc_server: ${{ vars.oc_server }}
          oc_token: ${{ secrets.oc_token }}
          commands: |
            cd charts/nr-oracle-service

            # uninstall if found
            helm uninstall ${{ github.event.repository.name }}-${{ github.event.number }} || true

            # Deploy Helm Chart (tag stays PR# — chart concatenates repo:tag, not @digest)
            helm dependency update
            helm package --app-version="${{ github.event.number }}" --version=${{ github.event.number }} .

            helm upgrade \
              --set-string app.envs.DB_HOST=${{ secrets.DB_HOST }} \
              --set-string app.envs.DB_NAME=${{ secrets.DB_NAME }} \
              --set-string app.envs.DB_PASSWORD=${{ secrets.DB_PWD }} \
              --set-string app.envs.DB_USER=${{ secrets.DB_USER }} \
              --set-string app.envs.DB_PORT="1543" \
              --set-string image.tag="${{ github.event.number }}" \
              --set-string namespace=${{ vars.oc_namespace }}  \
              --install --wait --atomic ${{ github.event.repository.name }}-${{ github.event.number }} \
              --values values.yaml \
              ./${{ github.event.repository.name }}-${{ github.event.number }}.tgz

            # print history
            helm history ${{ github.event.repository.name }}-${{ github.event.number }}

  results:
    name: Results
    if: always()
    needs: [builds, track, deploys]
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    steps:
      - name: Fail if any dependent job failed or was cancelled
        if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')
        run: exit 1
